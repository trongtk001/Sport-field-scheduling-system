server.port=8080

spring.datasource.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.datasource.url=jdbc:sqlserver://sport-field-scheduling-system-mssql-1:1433;databaseName=SportFieldSchedulingSystem;encrypt=true;trustServerCertificate=true;
spring.datasource.username=sa
spring.datasource.password=mssql1Ipw
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql = true

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.SQLServer2012Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = none
# spring.jpa.hibernate.ddl-auto = create

server.error.include-message=always

# App Properties
app.jwtCookieName= Authentication
app.jwtSecret= SecretKey
##60 * 60 * 1000 - 1 hour
app.jwtExpirationTime= 3600000
